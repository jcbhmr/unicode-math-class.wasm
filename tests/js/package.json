{
  "type": "module",
  "scripts": {
    "build": "make build",
    "postbuild": "npm install",
    "test": "node --test"
  },
  "workspaces": [
    "out"
  ],
  "devDependencies": {
    "@bytecodealliance/jco": "^0.14.2"
  }
}
